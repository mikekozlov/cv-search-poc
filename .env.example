# This is an example environment file.
# Copy this to .env and fill in your actual values.

# --- Client Toggle ---
# Set this to "True" to use AzureOpenAI, or "False" (or comment out) for standard OpenAI.
USE_AZURE_OPENAI="False"


# --- Standard OpenAI Settings ---
# (Required if USE_AZURE_OPENAI is "False")
# OPENAI_API_KEY="sk-proj-..."


# --- Azure OpenAI Settings ---
# (Required if USE_AZURE_OPENAI is "True")
# OPENAI_API_KEY="..."
# AZURE_ENDPOINT="https://your-resource-name.openai.azure.com/"
# AZURE_API_VERSION="2024-02-01"


# --- Model Names ---
# For OpenAI, this is the model name (e.g., "gpt-4o-mini").
# For Azure, this is your DEPLOYMENT name (e.g., "my-gpt4o-mini-deployment").
OPENAI_MODEL="gpt-5-mini"
# The embedding model.
OPENAI_EMBED_MODEL="text-embedding-3-large"
# Whisper / audio transcription model or Azure deployment name.
OPENAI_AUDIO_MODEL="whisper-1"


# --- Search Settings ---
# The default search mode. Options: "hybrid", "lexical", "semantic"
SEARCH_MODE="hybrid"

# --- Postgres DSNs ---
# Default DSNs align with docker-compose.pg.yml (localhost:5433).
DB_URL="postgresql://cvsearch:cvsearch@localhost:5433/cvsearch"

# --- Redis (Async Ingestion) ---
# Required for async ingestion watcher/extractor/enricher.
# Example includes a password and DB index; adjust for your local Redis.
# REDIS_URL="redis://:your-password@localhost:6379/0"


# --- Candidate Redaction (Optional) ---
# Salt to make anonymized names environment-specific.
# CANDIDATE_NAME_SALT="change-me"
# Prefix to use for anonymized candidate names.
# CANDIDATE_NAME_PREFIX="Candidate"


# --- GDrive Sync Settings (Optional) ---
# Path from 'rclone config file' (use forward slashes, e.g., C:/Users/Me/.config/rclone/rclone.conf)
# This is optional. If commented out, rclone uses its default path.
# GDRIVE_RCLONE_CONFIG_PATH=""

# The name you gave your remote in 'rclone config'
GDRIVE_REMOTE_NAME="gdrive"

# The folder on GDrive you want to sync FROM
GDRIVE_SOURCE_DIR="CV_Inbox"

# (Optional) The local folder to sync TO. Defaults to data/gdrive_inbox
# GDRIVE_LOCAL_DEST_DIR="data/gdrive_inbox"

